# Optional: URL of your deployed backend API (e.g. Render, Railway, EC2)
# If left unset, frontend automatically operates in offline demo mode with Assam dataset
VITE_API_BASE_URL=https://your-aquasentry-backend.onrender.com/api

# Optional: API key for backend requests
VITE_API_KEY=front_key_default
